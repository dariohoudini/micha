from django.apps import AppConfig


class PricingInventoryConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.pricing_inventory'
    label = 'pricing_inventory'
    verbose_name = 'Pricing & Inventory Intelligence'
